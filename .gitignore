# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/

# env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# misc
.DS_Store
*.pem

# drizzle
db/migrations/meta/
tsconfig.tsbuildinfo

# next.js autogenerated type declarations — regenerated by `next dev`/`next build`
next-env.d.ts

# test coverage output
coverage

# Local file uploads (STORAGE_DRIVER=local)
/uploads/

.krova-postgres

# Claude Code's own internal worktree state — ephemeral session working
# copies, never source. One such directory was previously committed by
# accident (.claude/worktrees/wf_cc78bb60-1c6-12/) and broke CI's Lint step
# on content that isn't part of the app; see docs/IMPLEMENTATION-REPORT.md.
.claude/worktrees/